---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-fixture
  labels:
    helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version | quote }}
data:
  fixture: "rh-push-helm-chart-to-registry-redhat-io e2e"
